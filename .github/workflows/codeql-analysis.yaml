name: "CodeQL"

on:
  push:
    branches: [ '*' ]
  schedule:
    - cron: '18 0 * * 4'

jobs:
  analyze:
    name: Analyze
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]
    uses: UBC-MOAD/gha-workflows/.github/workflows/codeql-analysis.yaml@ba82e1f5186369c8afb7dbeac4ec31af0bd4c2fe
    with:
      language: ${{ matrix.language }}
    secrets:
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_SALISHSEACAST_WEBHOOK_URL }}
